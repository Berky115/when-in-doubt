
class RenderTest extends React.Component{
    render(){
        // NOT JSX: Use a variable and if/else ternary
        return (
            // JSX PRINT results of Ternary expression with {}
        )
    }
}
