// setTimeout id = setTimeout(fn,delay) Initiates a timer that will execute the passed callback once after the specified delay has elapsed. A value that uniquely identifies the timer is returned.
// clearTimeout clearTimeout(id) Cancels (clears) the timer identified by the passed value if the timer hasn’t yet fired.
// setInterval id = setInterval(fn,delay) Initiates a timer that will continually try to execute the passed callback at the specified delay interval, until canceled. A value that uniquely identifies the timer is returned.
// clearInterval clearInterval(id) Cancels (clears) the interval timer identified by the passed value.
